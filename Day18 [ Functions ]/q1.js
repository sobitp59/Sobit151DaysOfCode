// Given an array of numbers, use map to double each element.
const numbers = [1, 2, 3, 4, 5];

const doubleElements = numbers.map(number => number * 2);
console.log(doubleElements); // [ 2, 4, 6, 8, 10 ]