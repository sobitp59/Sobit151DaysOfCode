// Given an array of strings, use map to return a new array with each string capitalized.

const strings = ['hello', 'world', 'javascript'];

const stringsUppercase = strings.map(string => string.toUpperCase());

console.log(stringsUppercase);