export function getImageUrl(place) {
    return (
      'https://i.imgur.com/' +
      place.imageId +
      'l.jpg'
    );
  }

// export const getImageUrl = (place) => {
//     return `https://i.ingur.com/${place.imageId}l.jpg`
//   }